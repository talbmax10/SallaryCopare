# نشر التطبيق على GitHub Pages 🚀

## المتطلبات
- ✅ حساب GitHub
- ✅ مستودع GitHub خاص بك (Repository)

---

## الخطوة 1️⃣: رفع المشروع إلى GitHub

### إذا كان لديك Git مثبت:

```bash
# إنشاء مستودع محلي (إذا لم تقم به)
git init

# إضافة جميع الملفات
git add .

# حفظ التغييرات
git commit -m "Initial commit: Excel Comparison App"

# إضافة رابط المستودع البعيد
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# رفع إلى GitHub
git push -u origin main
```

**تغييرات مهمة:**
- استبدل `YOUR_USERNAME` باسم المستخدم في GitHub
- استبدل `YOUR_REPO_NAME` باسم ال��ستودع (مثل: `salary-compare-app`)

### إذا لم يكن لديك Git:

1. افتح [GitHub.com](https://github.com)
2. اضغط على `+` في الأعلى > `New repository`
3. أدخل اسم المستودع
4. اضغط `Create repository`
5. ثم تحميل الملفات يدويا من خلال الواجهة

---

## الخطوة 2️⃣: تفعيل GitHub Pages

### الطريقة التلقائية (موصى بها):

1. اذهب إلى مستودعك على GitHub
2. اضغط على **Settings** (الإعدادات)
3. اذهب إلى **Pages** من القائمة اليسرى
4. تحت **Build and deployment**:
   - اختر **Source**: `GitHub Actions`
   - سيتم نشر التطبيق تلقائياً عند كل push على `main`

### الطريقة اليدوية:

1. اذهب إلى **Settings > Pages**
2. تحت **Source**, اختر:
   - Branch: `main`
   - Folder: `/(root)`
3. أو اختر branch و folder مختلفة حسب احتياجك

---

## الخطوة 3️⃣: البناء والنشر التلقائي

### اختيار 1: النشر التلقائي (موصى به)

إذا كنت قد فعلت **GitHub Actions**:
1. كل push إلى branch `main` سيقوم تلقائياً بـ:
   - بناء التطبيق
   - نشره على GitHub Pages

لا تحتاج لفعل شيء آخر! ✅

### اختيار 2: البناء المحلي والنشر:

إذا كنت تفضل بناء محلي:

```bash
# بناء التطبيق لـ GitHub Pages
pnpm build:client
```

أو على Windows:

```bash
pnpm build:client
```

ثم:
1. اذهب إلى **Settings > Pages**
2. اختر Branch: `main` و Folder: `/dist/spa`
3. اضغط Save

---

## الخطوة 4️⃣: الوصول إلى التطبيق

بعد النشر، سيكون التطبيق متاحاً على:

```
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
```

مثال:
```
https://talb-max10.github.io/salary-compare-app/
```

---

## ✅ التحقق من النشر

### طريقة 1: من صفحة الـ Repository

1. اذهب إلى مستودعك
2. اضغط على **Deployments** (على اليمين تحت الملفات)
3. ستجد نشرة GitHub Pages
4. اضغط على الرابط الأزرق

### طريقة 2: من صفحة Settings

1. اذهب إلى **Settings > Pages**
2. ستجد رسالة خضراء: "Your site is published at..."
3. اضغط على الرابط

---

## 🔄 التحديثات المستقبلية

بعد النشر الأول، كل ما عليك فعله:

```bash
# تعديل الملفات محلي��
# ثم:
git add .
git commit -m "Your message"
git push origin main
```

والباقي يتم تلقائياً! 🚀

---

## ⚙️ تخصيص مسار النشر

إذا أردت استخدام مسار مخصص (Subdirectory):

```bash
# تحديث المتغير البيئي
VITE_REPO_NAME=your-repo-name pnpm build:client
```

---

## ❌ حل المشاكل الشائعة

### مشكلة: الملفات لا تظهر بعد النشر

**الحل:**
1. تحقق من أن `dist/spa` موجود محلياً
2. اذهب إلى **Settings > Pages**
3. تأكد أن Branch هو `main`

### مشكلة: الأسلوب (CSS) لا يعمل

**الحل:**
1. تأكد من تعيين `base` في `vite.config.ts` (مضبوط بالفعل)
2. حاول إفراغ الكاش في المتصفح: `Ctrl+Shift+Del`

### مشكلة: صفحة 404

**الحل:**
1. تأكد من أن الملفات موجودة في `/dist/spa`
2. حاول إعادة النشر (push again)
3. انتظر 1-2 دقيقة لتحديث النشر

---

## 📊 ميزات النشر التلقائي

✅ تحديث تلقائي مع كل push  
✅ لا تحتاج لفعل شيء يدوي  
✅ HTTPS آمن  
✅ CDN سريع من GitHub  
✅ مجاني 100%  

---

## 💡 نصائح مهمة

1. **استخدم `main` أو `master`** كـ default branch
2. **تأكد من المتطلبات** قبل الرفع:
   ```bash
   pnpm typecheck
   pnpm test
   ```
3. **أضف `.gitignore`** (موجود بالفعل):
   ```
   node_modules/
   dist/
   .env
   ```

---

## 🎉 تم!

الآن تطبيقك يعمل على GitHub Pages!

### الخطوات الأساسية:
1. ✅ رفع المشروع إلى GitHub
2. ✅ تفعيل GitHub Pages
3. ✅ الانتظار 1-2 دقيقة
4. ✅ الوصول إلى الرابط

**استمتع بتطبيقك! 🚀**
