# تطبيق مقارن الرواتب - تعليمات التشغيل المحلي

## 📋 المتطلبات

قبل البدء، تأكد من تثبيت البرامج التالية على حاسوبك:

1. **Node.js** (الإصدار 16 أو أحدث)
   - [تنزيل من هنا](https://nodejs.org/)
   - تحقق من التثبيت: `node --version`

2. **Git** (لاختياري - إذا كنت تريد نسخ المشروع من GitHub)
   - [تنزيل من هنا](https://git-scm.com/)

---

## 🚀 خطوات التشغيل

### الخطوة 1️⃣: تنزيل المشروع

اختر أحد الطرق التالية:

#### الطريقة الأولى: تنزيل ملف ZIP
1. اضغط على زر "Download" في الصفحة
2. استخرج الملف في مجلد على حاسوبك

#### الطريقة الثانية: استخدام Git (اختياري)
```bash
git clone <رابط المشروع>
cd fusion-starter
```

---

### الخطوة 2️⃣: فتح Terminal/Command Prompt

#### على Windows:
- اضغط على `Win + R`
- اكتب `cmd` واضغط Enter
- ثم اكتب: `cd <مسار المجلد>`
  - مثال: `cd C:\Users\YourName\Desktop\fusion-starter`

#### على Mac/Linux:
- افتح Terminal من Applications > Utilities
- ثم اكتب: `cd <مسار المجلد>`
  - مثال: `cd ~/Desktop/fusion-starter`

---

### الخطوة 3️⃣: تثبيت المكتبات المطلوبة

في نفس نافذة Terminal، اكتب:

```bash
pnpm install
```

**ملاحظة:** إذا لم يكن `pnpm` مثبتا، استخدم بدلا منه:
```bash
npm install
```

**الوقت المتوقع:** 2-5 دقائق حسب سرعة الإنترنت

---

### الخطوة 4️⃣: تشغيل التطبيق

اكتب في نفس Terminal:

```bash
pnpm dev
```

أو إذا استخدمت npm:
```bash
npm run dev
```

ستظهر رسالة مثل:
```
  VITE v7.1.2  ready in XXX ms
  ➜  Local:   http://localhost:8080/
```

---

### الخطوة 5️⃣: فتح التطبيق في المتصفح

1. انسخ الرابط: `http://localhost:8080/`
2. افتح متصفحك (Chrome, Firefox, Safari, Edge)
3. الصق الرابط في شريط العنوان
4. اضغط Enter

**تم! ✅ التطبيق يعمل الآن على حاسوبك المحلي**

---

## 📝 كيفية استخدام التطبيق

### تحضير ملفات Excel

تأكد من أن ملفات Excel تحتوي على:
- **الصف الأول:** أسماء الأعمدة (مثل: الاسم، الراتب، الدرجة)
- **الصفوف التالية:** البيانات الفعلية

### خطوات المقارنة

1. **رفع الملفات:**
   - اضغط على "ملف الشهر الأول" ورفع الملف الأول
   - اضغط على "ملف الشهر الثاني" ورفع الملف الثاني

2. **اختيار مفتاح الربط:**
   - اختر الحقل الذي يربط بين الملفات (مثل: الاسم أو رقم الموظف)

3. **استثناء الأعمدة (اختياري):**
   - اختر الأعمدة التي لا تريد مقارنتها
   - استخدم "تحديد الكل" أو "إلغاء الكل" للتحكم السريع

4. **تنفيذ المقارنة:**
   - اضغط على زر "تنفيذ المقارنة"

5. **عرض النتائج:**
   - سترى الفروقات ملونة:
     - 🟢 **أخضر:** صفوف جديدة (مضافة)
     - 🔴 **أحمر:** صفوف محذوفة
     - 🟡 **أصفر:** صفوف متغيرة

6. **البحث:**
   - استخدم حقل البحث للبحث عن موظف معين

7. **الترتيب:**
   - اضغط على رأس العمود لترتيب النتائج

8. **التصدير:**
   - اضغط على "تصدير النتائج" لحفظ النتائج في ملف Excel جديد

---

## 🛑 إيقاف التطبيق

للتوقف عن تشغيل التطبيق:
- في نافذة Terminal، اضغط: `Ctrl + C`

---

## ⚙️ أوامر مفيدة أخرى

```bash
# بناء النسخة الإنتاجية (للنشر)
pnpm build

# تشغيل الاختبارات
pnpm test

# التحقق من أخطاء TypeScript
pnpm typecheck

# تنسيق الكود
pnpm format.fix
```

---

## ❓ حل المشاكل الشائعة

### المشكلة: "لا يمكن العثور على pnpm"
**الحل:**
```bash
npm install -g pnpm
```

### المشكلة: "Port 8080 مستخدم"
**الحل:** أغلق البرنامج الذي يستخدم المنفذ أو استخدم منفذ آخر

### المشكلة: "خطأ في تحميل الملف"
**الحل:**
- تأكد أن الملف بصيغة `.xlsx` أو `.xls`
- تأكد أن الصف الأول يحتوي على أسماء الأعمدة
- حاول استخدام ملف اختبار بسيط

### المشكلة: التطبيق بطيء جدا
**الحل:**
- أغلق البرامج الأخرى على حاسوبك
- استخدم متصفح أحدث (Chrome أو Firefox)
- تحقق من سرعة الإنترنت

---

## 📞 للمزيد من المساعدة

إذا واجهت أي مشكلة:
1. تحقق من أن Node.js مثبت بشكل صحيح
2. حاول مسح مجلد `node_modules` وتثبيت المكتبات مرة أخرى:
   ```bash
   rm -rf node_modules pnpm-lock.yaml
   pnpm install
   ```
3. تأكد من أن لديك الإنترنت متصل (للتنزيل الأول)

---

**تم! استمتع باستخدام التطبيق 🎉**
